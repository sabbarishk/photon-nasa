# Web server
fastapi
uvicorn[standard]
starlette
pydantic

# Notebook generation (workflow route)
Jinja2
nbformat

# HTTP client (HuggingFace API, NASA CMR) and URL data loading
requests

# Data loading and profiling
pandas

# Local embeddings -- installs torch, transformers, numpy transitively
sentence-transformers

# Vector similarity math (direct import in vector_store.py)
numpy

# Optional: Redis-backed distributed rate limiting.
# Falls back to in-memory rate limiting if Redis is unavailable.
redis

# Vector database — persistent HNSW index with metadata storage.
# Run `python -m photon.scripts.rebuild_index` once after install.
chromadb

# LLM code generation — calls Anthropic API with profile + methodology context.
# Requires ANTHROPIC_API_KEY environment variable (see .env.example).
anthropic
